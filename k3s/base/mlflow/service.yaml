apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: reviewsentinel
  labels:
    app: mlflow
    project: reviewsentinel
spec:
  selector:
    app: mlflow
  ports:
    - name: mlflow-ui
      port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
