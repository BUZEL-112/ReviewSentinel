apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
  namespace: reviewsentinel
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi    # Mistral 7B is ~4GB, 10GB gives room for additional models
