apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker/docker-compose.prod.yaml -o k8s/base/
    kompose.version: 1.31.2 (a92241f79)
  creationTimestamp: null
  labels:
    io.kompose.service: prefect-server
  name: prefect-server
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: prefect-server
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker/docker-compose.prod.yaml -o k8s/base/
        kompose.version: 1.31.2 (a92241f79)
      creationTimestamp: null
      labels:
        io.kompose.network/reviewsentinel-net: "true"
        io.kompose.service: prefect-server
    spec:
      containers:
        - image: ghcr.io/buzel-112/reviewsentinel-prefect-server:latest
          args: ["server"]
          name: reviewsentinel-prefect-server
          ports:
            - containerPort: 4200
              hostPort: 4200
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /root/.prefect
              name: prefect-data
      restartPolicy: Always
      volumes:
        - name: prefect-data
          persistentVolumeClaim:
            claimName: prefect-data
status: {}
