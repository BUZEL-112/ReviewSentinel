apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker/docker-compose.prod.yaml -o k8s/base/
    kompose.version: 1.31.2 (a92241f79)
  creationTimestamp: null
  labels:
    io.kompose.service: minio-init
  name: minio-init
spec:
  selector:
    matchLabels:
      io.kompose.service: minio-init
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker/docker-compose.prod.yaml -o k8s/base/
        kompose.version: 1.31.2 (a92241f79)
      creationTimestamp: null
      labels:
        io.kompose.network/reviewsentinel-net: "true"
        io.kompose.service: minio-init
    spec:
      restartPolicy: OnFailure
      containers:
        - command:
            - /bin/sh
            - -c
            - ' sleep 5; mc alias set myminio http://minio:9000 reviewsentinel changeme123; mc mb myminio/reviewsentinel-artifacts || true; mc anonymous set public myminio/reviewsentinel-artifacts; '
          env:
            - name: MINIO_ROOT_PASSWORD
              value: changeme123
            - name: MINIO_ROOT_USER
              value: reviewsentinel
          image: minio/mc:latest
          name: minio-init
          resources: {}
      restartPolicy: Always
status: {}
